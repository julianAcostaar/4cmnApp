import 'package:flutter/material.dart';
 
class SelectorPerfil extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Selector de usuarios')
      ),
    );
  }
}